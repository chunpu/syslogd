{
  "name": "syslogd",
  "version": "1.1.2",
  "description": "syslog server and parser",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "$(which node) test/test.stream.js && $(which node) test && $(which node) test/tcp.js && $(which node) test/tcp.multi.js",
    "performance": "node performance"
  },
  "keywords": [
    "syslog",
    "parser",
    "server"
  ],
  "author": "ft",
  "license": "ISC",
  "dependencies": {
    "debug": "^2.1.0"
  },
  "devDependencies": {
    "mocha" : "3.4.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/chunpu/syslogd.git"
  },
  "bugs": {
    "url": "https://github.com/chunpu/syslogd/issues"
  },
  "homepage": "https://github.com/chunpu/syslogd"
}
